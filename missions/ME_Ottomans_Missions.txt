ME_Ottomans_missions_1 = { 
	slot = 1 
	ai = yes 
	generic = no 
	potential = { 
		tag = TUR 
		has_country_flag = use_missions_expanded_missions_instead_of_paradox 
	} 
	has_country_shield = yes 

	TUR_madman_rise = { 
		icon = me_mission_ottomans_balkan_domination 
		required_missions = { 
			me_crusade_aftermath
		}
		position = 2
		provinces_to_highlight = {
			area = wallachia_area
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = { 
			OR = {
				if = {
					limit = {
						is_expanded_mod_active = { mod = subjects }
					}
					WAL = { 
						is_subject_of_type = muslim_tributary_vassal
						is_subject_of = ROOT
					}
				}
				else = {
					WAL = { 
						is_subject_of = ROOT
						is_vassal = yes
					}
				}
				wallachia_area = {
					type = all
					country_or_non_sovereign_subject_holds = ROOT
				}
			}
		}
		effect = { 
			if = {
				limit = {
					WAL = { is_subject_of = ROOT }
				}
				WAL = {
					add_country_modifier = {
						name = ME_Ottoman_Suzeranity
						duration = -1
					}
				}
				custom_tooltip = ME_Ottoman_Supplying_Army_TT
				tooltip = {
					WAL = {
						add_country_modifier = {
							name = ME_Ottoman_supplying_army
							duration = -1
						}	
					}
				}
			}
			else = {
				wallachia_area = {
					add_province_modifier = {
						name = ME_Ottoman_Balkan_Frontier
						duration = -1
					}
				}
			}
			if = {
				limit = {
					exists = MOL
				}
				add_casus_belli = {
					type = cb_vassalize_mission
					target = MOL
				}
			}
			else = {
				moldavia_area = {
					limit = {
						NOT = { owned_by = ROOT }
						NOT = { is_core = ROOT }
					}
					add_permanent_claim = ROOT
				}
			}
		}
	}
}