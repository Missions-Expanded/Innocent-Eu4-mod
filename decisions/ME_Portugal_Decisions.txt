country_decisions = {
	por_organize_crusade_war = {
		major = yes
		potential = {
			tag = POR
			has_country_flag = por_crusade_partner
			any_known_country = { has_country_flag = por_crusade_partner }
		}
		allow = {
			is_in_war = {
				attacker_leader = ROOT
				defender_leader = event_target:por_crusade_target
				casus_belli = cb_vassalize_mission
			}
			custom_trigger_tooltip = {
				tooltip = por_crusade_partner_tt
				is_in_war = {
					attacker_leader = ROOT
					defender_leader = event_target:por_crusade_target
					casus_belli = cb_vassalize_mission
				}
				all_known_country = {
					OR = {
						NOT = {
							is_in_war = { attacker_leader = ROOT }
						}
						is_in_war = {
							attacker_leader = ROOT
							defender_leader = event_target:por_crusade_target
							casus_belli = cb_vassalize_mission
						}
					}
				}
			}
			any_known_country = {
				custom_trigger_tooltip = {
					tooltip = por_crusade_partner
					has_country_flag = por_crusade_partner
				}
				NOT = { is_at_war = yes }
				NOT = { truce_with = event_target:por_crusade_target }
			}
		}
		effect = {
			every_known_country = {
				limit = {
					has_country_flag = por_crusade_partner
					NOT = { is_at_war = yes }
					NOT = { truce_with = event_target:por_crusade_target }
				}
				custom_tooltip = alb_organize_crusade_war_tt
				country_event = {
					id = ME_Portugal_Events.24
					days = 0
				}
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
}
