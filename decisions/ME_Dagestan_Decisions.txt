country_decisions = {
	form_dagestan_nation = {
		major = yes
		potential = {
			primary_culture = dagestani
			NOT = { has_country_flag = formed_dagestan_flag }
		}
		provinces_to_highlight = {
			area = dagestan_area
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			dagestan_area = {
				type = all
				owned_by = ROOT
				is_core = ROOT
			}
		}
		effect = {
			override_country_name = DAG
			set_country_flag = formed_dagestan_flag
			swap_non_generic_missions = yes
			add_prestige_or_monarch_power = { amount = 25 }
			add_country_modifier = {
				name = centralization_modifier
				duration = 9125
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
}