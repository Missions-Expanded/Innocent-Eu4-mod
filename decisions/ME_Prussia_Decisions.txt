country_decisions = {
	# PRU_decision_triumph = {
		# major = yes
		# potential = {
			# tag = PRU
			# NOT = { has_country_flag = PRU_triumph }
			# mission_completed = PRU_austria
		# }
		# provinces_to_highlight = {
			# NOT = { owned_by = ROOT }
			# OR = {
				# region = north_german_region
				# region = south_german_region
			# }
			# NOT = {
				# OR = {
					# area = erzgebirge_area
					# area = bohemia_area
					# area = tirol_area
					# area = moravia_area
					# area = inner_austria_area
					# area = austria_proper_area
					# area = romandie_area
					# area = carinthia_area
					# area = switzerland_area
				# }
			# }
		# }
		# allow = {
			# num_of_owned_provinces_with = {
				# OR = {
					# region = north_german_region
					# region = south_german_region
				# }
				# NOT = {
					# OR = {
						# area = erzgebirge_area
						# area = bohemia_area
						# area = tirol_area
						# area = moravia_area
						# area = inner_austria_area
						# area = austria_proper_area
						# area = romandie_area
						# area = carinthia_area
						# area = switzerland_area
					# }
				# }
				# value = 90
			# }
		# }
		# effect = {
			# country_event = { id = ME_Prussia_Events.25 }
			# set_country_flag = PRU_triumph
		# }
		# ai_will_do = {
			# factor = 1
		# }
	# }
}