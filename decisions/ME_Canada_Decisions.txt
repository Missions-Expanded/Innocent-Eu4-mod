country_decisions = {
	can_the_expedition = {
		major = yes
		potential = {
			tag = CAN
			OR = {
				NOT = {
					has_country_flag = can_enable_expeditions
				}
				had_country_flag = {
					flag = can_enable_expeditions
					days = 1825
				}
			}
		}
		allow = {
			tag = CAN
			has_country_flag = can_enable_expeditions
		}
		effect = {
			clr_country_flag = can_enable_expeditions
			set_country_flag = can_enable_expeditions
			add_prestige = 200
		}
	}
	can_search_for_gold = {
		major = yes
		potential = {
			tag = CAN
			has_country_flag = earch_for_gold_decision_enabled
		}
		allow = {
			tag = CAN
			has_country_flag = earch_for_gold_decision_enabled
		}
		effect = {
			add_prestige = 200
		}
	}
}