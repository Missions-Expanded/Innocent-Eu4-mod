namespace = ME_Epirus_Events

country_event = { #The Fate of Epirus
	id = ME_Epirus_Events.1
	title = ME_Epirus_Events.EVTNAME1
	desc = ME_Epirus_Events.EVTDESC1
	picture = COURT_eventPicture

	is_triggered_only = yes

	option = {
		name = ME_Epirus_Events1.EVTOPT1
		ai_chance = {
			factor = 50
		}
		set_country_flag = epirus_ancient_root
		swap_non_generic_missions = yes
	}

	option = {
		name = ME_Epirus_Events1.EVTOPT2
		ai_chance = {
			factor = 50
		}
		set_country_flag = epirus_despotate_root
		swap_non_generic_missions = yes
	}
}

country_event = { # Alexander or not?
	id = ME_Epirus_Events.2
	title = ME_Epirus_Events.EVTNAME2
	desc = ME_Epirus_Events.EVTDESC2
	picture = COURT_eventPicture

	mean_time_to_happen = {
		months = 3
	}
	
	trigger = {
		culture_group = byzantine
		owns = 148
		is_at_war = no
		NOT = {
			has_country_flag = allow_macedon
		}
		NOT = {
			has_country_flag = rejected_macedon
		}
	}

	option = {
		name = ME_Epirus_Events2.EVTOPT1
		ai_chance = {
			factor = 50
		}
		set_country_flag = allow_macedon
	}

	option = {
		name = ME_Epirus_Events2.EVTOPT2
		ai_chance = {
			factor = 50
		}
		set_country_flag = rejected_macedon
	}
}