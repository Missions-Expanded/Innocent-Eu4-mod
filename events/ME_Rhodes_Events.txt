#Made by SharkFins#
###################
namespace = ME_Rhodes_Events

country_event = {
	id = ME_Rhodes_Events.1
	title = ME_Rhodes_Events.EVTNAME0
	desc = ME_Rhodes_Events.EVTDESC0
	picture = COURT_eventPicture
	
	is_triggered_only = yes
	
	option = {
		name = ME_Rhodes_Events.EVTOPT1
		
		ai_chance = { factor = 0.25 }
		
		if = {
			limit = {
				has_dlc = "Dharma"
			}
			change_government = monarchy
		}
		else = {
			set_legacy_government = feudal_monarchy
		}
		add_stability = 1
	}
	option = {
		name = ME_Rhodes_Events.EVTOPT2
		
		ai_chance = { factor = 0.25 }
		
		if = {
			limit = {
				AND = {
					has_dlc = "Dharma"
					has_dlc = "Golden Century"
				}	
			}
			change_government = republic
			add_government_reform = pirate_republic_reform
		}
		else = {
			set_legacy_government = republican_dictatorship
		}
		add_stability = -1
	}
	option = {
		name = ME_Rhodes_Events.EVTOPT3
		
		ai_chance = { factor = 0.25 }
		
		if = {
			limit = {
					has_dlc = "Dharma"
				}
			remove_government_reform = monastic_order_reform
			set_government_rank = kingdom
		}
		else = {
			set_legacy_government = clerical_state
		}
		add_stability = 1
	}
	option = {
		name = ME_Rhodes_Events.EVTOPT4
		
		ai_chance = { factor = 0.25 }
		
		if = {
			limit = {
				has_dlc = "Dharma"	
			}
			change_government = republic
		}
		else = {
			set_legacy_government = oligarchic_republic
		}
		add_republican_tradition = 50
	}
}