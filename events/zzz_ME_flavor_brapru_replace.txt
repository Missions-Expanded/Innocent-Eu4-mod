#namespace = flavor_brapru


# Berliner Unwille
# country_event = {
	# id = flavor_brapru.9
	# title = "flavor_brapru.EVTNAME1"
	# desc = "flavor_brapru.EVTDESC1"
	# picture = CITY_DEVELOPMENT_eventPicture
	
	# trigger = {
		# NOT = { has_country_flag = BRA_unwille_happened }
		# OR = {
			# tag = BRA
			# tag = PRU
		# }
		# owns = 50	# Berlin
		# is_year = 1448
		# NOT = { is_year = 1475 }
		# has_regency = no
	# }
	
	# mean_time_to_happen = {
		# months = 80
	# }
	# immediate = {
		# set_country_flag = BRA_unwille_happened
	# }
	
	# option = {
		# name = "flavor_brapru.EVTOPTA1" #Ignore the rabble
		# 50 = {	create_revolt = 1 }
		# if = {
			# limit = {
				# has_country_flag = use_missions_expanded_missions_instead_of_paradox
			# }
			# custom_tooltip = BRA_CAN_COMPLETE_MISSION_2
			# set_country_flag = ME_dealt_with_berliner_unwille
		# }
	# }
	# option = {
		# name = "flavor_brapru.EVTOPTB1" #Listen to the people
		# add_ruler_modifier = { name = bra_berliner_unwille }
	# }
# }

#Hohenzollers of Brandenburg
country_event = {
	id = flavor_brapru.23
	title = flavor_brapru.23.t
	desc = flavor_brapru.23.d
	picture = GOOD_WITH_MONARCH_eventPicture
	
	fire_only_once = yes
	is_triggered_only = yes
	
	trigger = {
		NOT = { has_global_flag = flavor_brapru_23_happened }
	}
	
	immediate = {
		hidden_effect = {
			set_global_flag = flavor_brapru_23_happened
		}
	}
	
	option = { #Great!
		name = flavor_brapru.23.a
		
		ai_chance = { factor = 100 }
		
		tooltip = {
			BRA = { create_union = ANS }
		}
		custom_tooltip = flavor_brapru.23.a.tt
		BRA = {
			country_event = { id = flavor_brapru.24 } #Succession of Albrecht Achilles
		}
	}
	
	option = { #Great!
		name = flavor_brapru.23.b
		
		ai_chance = { factor = 0 }
		
		define_ruler = {
			dynasty = "von Hohenzollern"
		}
		if = {
			limit = {
				has_states_general_mechanic = yes
			}
			change_statists_vs_orangists = 1
		}
	}
	
	option = { #Extra_ME_OPTION
		name = flavor_brapru.23.ME
		
		ai_chance = { factor = 10000 }
		
		trigger = {
			mission_completed = ans_hohenzollern
			BRA = {
				ai = yes
				was_player = no
			}
		}
		
		create_union = BRA
		add_historical_friend = BRA
		add_truce_with = BRA
		BRA = {
			add_historical_friend = ROOT
			add_truce_with = ROOT
		}
	}
}
country_event = {
	id = flavor_brapru.36
	title = "flavor_brapru.36.t"
	desc = "flavor_brapru.36.d"
	picture = MILITARY_CAMP_eventPicture
	
	fire_only_once = yes
	
	trigger = {
		OR = {
			has_country_flag = PRU_kanton
			AND = {
				tag = PRU
				government = monarchy
				MIL = 5
				is_year = 1700
				NOT = { is_year = 1800 }
			}
		}
	}
	
	mean_time_to_happen = {
		months = 300
	}
	
	option = {
		name = "flavor_brapru.36.a"
		custom_tooltip = flavor_brapru.36.a.tt
		ai_chance = { factor = 100 }
		add_stability = -1 #Administrative Friction
		add_army_professionalism = 0.05
		if = {
			limit = { has_dlc = "Rights of Man" }
			add_militarised_society = 40
		}
		if = {
			limit = {
				NOT = {
					has_dlc = "Rights of Man"
				}
			}
			add_mil_power = 100
		}
		add_yearly_manpower = 1
	}
	option = {
		name = "flavor_brapru.36.b"
		ai_chance = { factor = 0 }
		custom_tooltip = flavor_brapru.36.b.tt
		add_years_of_income = 1
		add_stability_or_adm_power = yes #Keeping the Status Quo
	}
}
