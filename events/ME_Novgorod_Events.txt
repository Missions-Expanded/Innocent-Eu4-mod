namespace = ME_Novgorod_Events

country_event = {
	id = ME_Novgorod_Events.1
	title = ME_Novgorod_Events.1.T
	desc = ME_Novgorod_Events.1.D
	picture = GOOD_WITH_MONARCH_eventPicture

	is_triggered_only = yes

	option = {
		name = ME_Novgorod_Events.1.OPT1
		ai_chance = {
			factor = 1
		}

		change_tag = RUS
		swap_non_generic_missions = yes
		remove_non_electors_emperors_from_empire_effect = yes
		if = {
			limit = {
				NOT = { government_rank = 3 }
			}
			set_government_rank = 3
		}
		russia_region = {
			limit = {
				NOT = { owned_by = ROOT }
			}
			add_permanent_claim = RUS
		}
		crimea_region = {
			limit = {
				NOT = { owned_by = ROOT }
			}
			add_permanent_claim = RUS
		}
		ural_region = {
			limit = {
				NOT = {
					owned_by = ROOT
				}
				is_empty = no
			}
			add_permanent_claim = RUS
		}
		add_country_modifier = {
			name = "centralization_modifier"
			duration = 7300
		}
		if = {
			limit = {
				has_custom_ideas = no
			}
			country_event = { id = ideagroups.1 } #Swap Ideas
		}
		set_country_flag = formed_russia_flag
		add_prestige = 25
	}

	option = {
		name = ME_Novgorod_Events.1.OPT2
		ai_chance = {
			factor = 0
		}

		add_stability = 1
		add_adm_power = 100
		add_dip_power = 100
		add_mil_power = 100
	}
}